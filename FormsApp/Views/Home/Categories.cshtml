@model IEnumerable<FormsApp.Models.Category>

<h2>Kategoriler</h2>

<ul class="list-group">
    @foreach (var category in Model)
    {
        <li class="list-group-item">
            <a asp-action="Index" asp-route-id="@category.Id">@category.Name</a>
        </li>
    }
</ul>
